imports:
    - { resource: config_test.yaml }

orbitale_cms:
    layouts:
        front: { resource: "test_layout.html.twig" }
        hostSpecific: { resource: "test_layout_host.html.twig", host: "local.host" }
